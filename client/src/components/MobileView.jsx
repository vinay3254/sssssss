import React from 'react';

const MobileView = () => {
  return (
    <div className="md:hidden fixed bottom-4 right-4 bg-primary-500 text-white p-2 rounded-lg text-xs">
      Mobile optimized
    </div>
  );
};

export default MobileView;